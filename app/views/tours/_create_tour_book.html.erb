<div class="modal fade" id="createTourBookModal" tabindex="-1" role="dialog" aria-labelledby="createTourBookModalCenter" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <%= form_with(url: user_tour_books_path, scope: 'tour_book') do |f| %>

        <%= hidden_field_tag 'item_id', @tour.id %>

        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Create new TourBook</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <div class="form-errors">
            <%= form_errors_for f.object %>
          </div>
          <div class="form-group">
            <label for="create_tour_book_name" class="col-form-label">Name</label>
            <input type="text" class="form-control" id="create_tour_book_name" name="tour_book[name]">
          </div>
          <div class="form-group">
            <label for="create_tour_book_description" class="col-form-label">Description</label>
            <textarea class="form-control" id="create_tour_book_description" name="tour_book[description]"></textarea>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
          <button type="submit" class="btn btn-danger">Create</button>
        </div>
      <% end %>
    </div>
  </div>
</div>